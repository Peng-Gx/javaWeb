<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mybatis.Mapper">
    <select id="selectStudent" resultType="mybatis.Student">
        select * from student
    </select>
    <select id="selectStudentToMap" resultType="Map">
        select * from student
    </select>
</mapper>